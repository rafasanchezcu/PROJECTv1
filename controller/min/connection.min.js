var factory=new markerPaqueteFactory,mapa=new initMap;jQuery(document).ready(function(a){a.getJSON("http://localhost:8000/data/coordenadas.json").success(function(e){a.coordenadas=e,mapa.graficarEstaciones(e)})});